*filter

<% @allowed_customers.each do |name, dataHash| %><% if dataHash['public_ip'] %>
-A INPUT -p tcp -s <%= dataHash['public_ip'] %> --dport <%= @port %> -j ACCEPT -m comment --comment "rsyslog <%= name %>"
<% end %><% end %>

COMMIT
